
# A gitignore file specifies intentionally untracked files that Git should 
# ignore. Files already tracked by Git are not affected; see the official note 
# below for details [git ducumentation](https://git-scm.com/docs/gitignore).

# Git actually ignores all files inside the current directory but this file.

*
!.gitignore